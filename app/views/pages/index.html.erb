<%

%>

<%

citylist = [
"Ocala"
]

citylist.each { |a|
  @client.search(a, result_type: "recent").take(100).collect do |tweet|
    tweetWords = tweet.text.split(' ')
      if tweetWords.first != "RT"
        tweetWords.each do |tweetword| %>  
          <%= tweetword %><br/> 
<%      
        end
      end
    end
}
%>